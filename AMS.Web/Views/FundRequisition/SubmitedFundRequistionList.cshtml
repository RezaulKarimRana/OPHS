@model IEnumerable<AMS.Models.ServiceModels.FundRequisition.FundRequisitionVM>

@{
    ViewData["Title"] = "Complete Budget List";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}
<div class="card shadow mb-4">
    <div class="card-header">

        <a id="btnReloadData" href="#" class="float-left">
            <span class="">
               Submitted  Fund Requistion List
            </span>
        </a>
        <a id="btnReloadData" href="#" class="btn btn-primary float-right">
            <span class="icon text-white">
                <i class="fas fa-sync"></i>
            </span>
        </a>
    </div>
    <div class="card-body">
        <div class="datatable">
            <div class="col-sm-12">
                <table class="table table-bordered table-hover table-striped dataTable" id="dataTable" width="100%" cellspacing="0" role="grid" aria-describedby="dataTable_info" style="width: 100%;">
                    <thead class="thead-dark">
                        <tr>
                            <th>SL.</th>                        
                            <th width="50%">Estimate Details</th>
                           
                            <th>Requistion Details</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody class="text-left">
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</div>


@section Scripts{

    <script>
        $(document).ready(function () {
            $('#dataTable').dataTable({
                searchHighlight: true,
                order: [[0, "desc"]],

                order: [[0, "desc"]],
                ajax: {
                    url: "/FundRequisition/SubmitedFundRequistionList",
                    type: 'POST',
                    data: function (d) {
                    }
                }
            });
        });

        $(document).on("click", "#btnReloadData", function () {
            location.reload();
        });
    </script>
}
